name: RDP

on:
  workflow_dispatch:

jobs:
{% for i in range(1,21) %}
  rdp-{{i}}:
    name: RDP-Session-{{i}}
    runs-on: windows-latest
    timeout-minutes: 360   # 6 HOURS LIMIT
    outputs:
      ip: ${{ steps.getip.outputs.tsip }}

    steps:

      - name: Configure RDP
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

      - name: Create RDP User
        run: |
          $securePass = ConvertTo-SecureString "m}O57Y80DJ)gs>e+" -AsPlainText -Force
          if (-not (Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
            New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
            Add-LocalGroupMember -Group "Administrators" -Member "RDP"
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          }

      - name: Install Tailscale
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $path = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $path
          Start-Process msiexec.exe -ArgumentList "/i `"$path`" /quiet /norestart" -Wait

      - name: Tailscale Login + Get IP
        id: getip
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} `
            --hostname=gh-rdp-{{i}}-$env:GITHUB_RUN_ID

          $tsIP = ""
          while ($tsIP -eq "") {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            Start-Sleep 3
          }

          echo "tsip=$tsIP" >> $env:GITHUB_OUTPUT

      - name: Keep Alive (6 Hours)
        run: |
          Write-Host "RDP READY:"
          Write-Host "IP: ${{ steps.getip.outputs.tsip }}"
          Write-Host "USERNAME: RDP"
          Write-Host "PASSWORD: m}O57Y80DJ)gs>e+"
          for ($i=0; $i -lt 360; $i++) { Start-Sleep -Seconds 60 }
{% endfor %}

  collect-ips:
    name: 📌 All RDP Tailscale IPs (One Place)
    runs-on: ubuntu-latest
    needs:
{% for i in range(1,21) %}
      - rdp-{{i}}
{% endfor %}
    steps:
      - name: Show All IPs in List
        run: |
          echo "====== ALL RDP IPs ======"
{% for i in range(1,21) %}
          echo "RDP-{{i}} → ${{ needs.rdp-{{i}}.outputs.ip }}"
{% endfor %}
          echo "=========================="
          echo "USERNAME: RDP"
          echo "PASSWORD: m}O57Y80DJ)gs>e+"
